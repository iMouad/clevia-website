// Root layout — minimal shell required by Next.js.
// The locale-aware layout lives in app/[locale]/layout.tsx.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
